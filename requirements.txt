# Runtime dependencies (what the bot needs to run).
python-telegram-bot==22.8
requests>=2.31

# NOTE: Pillow is not required. The original single-file bot imported it only to
# build a throwaway test image for the admin channel; the refactor sends the
# bundled brand photo instead.
